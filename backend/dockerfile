FROM node:20.19-alpine

ENV DATABASE_URL=postgresql://postgres:postgres@postgres-db:5432/todo_db
ENV PORT=4450

WORKDIR /app

COPY package.json package-lock.json ./

RUN npm ci --only=production

COPY ./src ./src

EXPOSE 4450

CMD ["npm", "start"]
