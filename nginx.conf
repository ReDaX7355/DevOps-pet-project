events {}

http {
	server {
		listen 80;
	
		location / {
			proxy_pass http://frontend:80;
		}

		location /api/ {
			proxy_pass http://app_backend:5000/api/;
		}

		location /nginx_status {
			stub_status;
			access_log off;
			allow 192.168.0.0/16;  # сеть Docker
			allow all;
		}
	
	}
}
